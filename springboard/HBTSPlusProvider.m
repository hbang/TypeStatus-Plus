#import "HBTSPlusProvider.h"

@implementation HBTSPlusProvider

- (void)showNotification:(NSString *)text {
	HBLogDebug(@"If the API were working right now, we'd be showing a status bar notification with the following text: %@", text);
}

@end